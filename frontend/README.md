# Edukeys — Frontend

Socle React 19 + TypeScript strict (T-11). Voir `CLAUDE.md` pour les règles
de ce module et `../CLAUDE.md` pour le contexte projet.

## Démarrage

```bash
npm install
cp .env.example .env      # VITE_API_BASE_URL, par défaut http://localhost:8080
npm run dev
```

## Commandes

Voir `CLAUDE.md`.
