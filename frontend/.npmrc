# openapi-typescript (peer: typescript ^5.x) et quelques packages de test ne
# suivent pas encore le TypeScript ~6.x fourni par ce template Vite — vrai
# conflit de peer, pas une erreur d'installation. legacy-peer-deps évite que
# `npm ci` échoue en CI comme en local pour cette seule raison.
legacy-peer-deps=true
