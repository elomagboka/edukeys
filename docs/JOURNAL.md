# Journal de développement

> Rempli automatiquement par la commande `/point` en fin de session.
> Se lit au démarrage de la session suivante : il remplace l'historique de conversation.

## 2026-08-28 - T-09 : Partie GitHub faite. Services Render reportés, blueprint détaché - les identifiants de plan du render.yaml sont périmé, à réécrire au premier déploiement réel.


## 2026-08-28 — T-06 (audit et historisation) + T-07 (documentation API) — PR #69 ouverte

- Fait : **`@Audited` (Hibernate Envers) posé sur les cinq entités métier concrètes** (`Etablissement`, `Utilisateur`, `AffectationEtablissement`, `JetonRafraichissement`, `DemoEntite`), table `revisions` portant l'auteur (résolu comme `creePar`/`modifiePar`) ; `HistoriqueService` générique (`common/audit`) avec garde-fou explicite contre l'angle mort signalé en T-05 (les tables `_aud` échappent au filtre multi-établissement — vérifié désormais contre le contexte courant, testé en 404 inter-établissement) ; démonstration bout en bout sur `GET /internal/demo/entites/{id}/historique`. **T-07** : `OpenApiConfig` (schéma JWT `bearerAuth`, endpoints publics annulés via `@SecurityRequirements`), Swagger UI restreint au profil `local`, `/v3/api-docs` ouvert en `test` pour `OpenApiExportTest` qui écrit `target/openapi.json` au build. **143 tests, `mvn clean verify` vert.**
- Reste : T-08 à T-11 non entamés. `OpenApiConfig` marque `bearerAuth` en sécurité par défaut sur toute l'API mais aucun endpoint métier réel (hors `identite`) n'existe encore pour vérifier le rendu Swagger en pratique — à surveiller dès T-10. Toujours ouvert : issue #58 (rate limiting), ADR-0008 (stockage jetons), hypothèse « Render écrase X-Forwarded-For » non vérifiée, `AsyncConfig`/`ContexteEtablissementTaskDecorator` non couverts (issue #66).
- Décisions : **`@Audited` posé sur `BaseEntity` seul ne suffit pas** — constaté à l'exécution (`NotAuditedException`) sur les cinq entités existantes, y compris celles à un seul niveau d'héritage sous `BaseEntity` : l'annotation doit être répétée sur chaque classe concrète. `VerificateurAuditEnvers` (nouveau, même philosophie que `VerificateurFiltreEtablissement` de T-05) fait échouer le démarrage si une entité l'oublie plutôt que de compter sur la vigilance en revue. `springdoc-openapi` remonté de 2.6.0 à 2.8.6 : la version figée dans `docs/SPRINT-0.md`/T-00 est incompatible avec Spring Framework 6.2 (`NoSuchMethodError` sur `ControllerAdviceBean`) — SPRINT-0.md autorisait explicitement cette bascule sans toucher à Spring Boot.
- Piège rencontré : **la branche `appmod/java-upgrade-20260827191244` portait, non commité, un repassage du projet en Java 25** — contraire à l'instruction explicite de `CLAUDE.md` ajoutée après l'incident ArchUnit du retour à 21 (commit `7cbd2a5`). Détecté avant tout travail (diff du working tree lu avant de committer quoi que ce soit) ; mis de côté par `git stash` (non perdu, non inclus dans la PR) et travail reparti depuis `main` (Java 21). Nécessite une décision explicite de l'utilisateur avant de rejouer cet outil de modernisation — ne pas le relancer sans vérifier d'abord s'il est piloté par un processus externe encore actif.

## 2026-08-27 — Revue T-05 (suite) + CI démarrage conteneur — PR #67, #68 mergées

- Fait : **3 points de revue T-05 fermés** (PR #67) — `UtilisateurService.obtenir(UUID)` renommé `obtenirSoiMeme(UtilisateurPrincipal)` (signature ne permet plus de demander un autre compte que le sien) ; `DetecteurFuiteContexteEnregistrementTest` prouve en HTTP réel (MockMvc + endpoint de test dédié profil `test`) que le filtre de fuite est tissé dans la chaîne servlet ; R4.1/R4.2 prouvées par mutation d'intégration (C9, C10, persistance réelle contre PostgreSQL) au même niveau que R4.3/R4.4. **CI durcie** (PR #68) — étape post-`docker build` qui lance réellement le conteneur et vérifie qu'il démarre (bannière + `Starting EdukeysApplication`) sans `UnsupportedClassVersionError` ni `no main manifest attribute`. **140 tests, verts.**
- Reste : `AsyncConfig`/`ContexteEtablissementTaskDecorator`/`TaskScheduler` toujours non couverts, reportés en **issue #66**, à traiter au premier `@Async` réel (US-25/US-28). **T-06 (audit et historisation)** reste la suite logique de T-05, non entamée. Toujours ouvert : issue #58 (rate limiting), ADR-0008 (stockage jetons, bloque T-11), hypothèse « Render écrase X-Forwarded-For » non vérifiée.
- Décisions : **suppression du profil Spring par défaut** (`spring.profiles.active: local` retiré d'`application.yml`) — un défaut implicite avait fait tourner le conteneur de vérification CI avec le profil qui charge `db/testdata` (comptes de démonstration, mots de passe connus) sans que personne ne l'ait demandé. `VerificateurProfilActif` (`ApplicationRunner`) exige désormais `SPRING_PROFILES_ACTIVE` explicite, même règle que `EDUKEYS_JWT_SECRET`. **Garde-fou supplémentaire en `EnvironmentPostProcessor`** (`GardeProfilConteneurEnvironmentPostProcessor`, pas un `ApplicationRunner`) : refuse `local`/`test` dans un conteneur (détecté via `/.dockerenv`) *avant* la création du contexte Spring — donc avant Flyway — parce que le scénario grave n'est pas « mauvais profil au démarrage » mais « `local` pointe vers une vraie base et y sème des comptes de démo avant qu'aucun garde postérieur ne puisse intervenir ». `mvn spring-boot:run` nécessite maintenant `-Dspring-boot.run.profiles=local` — **une correction ultérieure du CLAUDE.md par l'utilisateur a annulé cette mise à jour de la commande documentée** (commit `7cbd2a5`, qui a aussi ajouté une note verrouillant Java 21) : à vérifier en prochaine session si la commande `mvn spring-boot:run` telle que documentée fonctionne encore sans le flag.
- Piège rencontré : **`EnvironmentPostProcessor` n'est pas éligible au mécanisme `META-INF/spring/*.imports`** (réservé à `AutoConfiguration`) — première tentative d'enregistrement via ce fichier silencieusement ignorée par Spring (aucune erreur, le garde ne se déclenchait juste jamais). Détecté en testant manuellement le scénario `local`-en-conteneur et en constatant l'absence de rejet. Corrigé via l'ancien `META-INF/spring.factories`. Par ailleurs, **`.dockerignore` ne bloquait pas `application-local.yml`** (gitignoré mais pas dockerignoré) — trouvé en vérifiant ce correctif : le fichier existait sur ce poste avec une URL `localhost:5432` en dur, et Docker ne respecte pas `.gitignore`, donc l'aurait copié tel quel dans l'image si un dev en avait un rempli de vrais identifiants. Ajouté à `.dockerignore`.

## 2026-08-25 (suite) — Retour de Java 25 à Java 21

- Fait : **retour du projet à Java 21** (`pom.xml` racine, `backend/Dockerfile` — image de build `maven:3.9-eclipse-temurin-21` et image d'exécution `eclipse-temurin:21-jre-jammy`, `.github/workflows/ci.yml` — `setup-java` `java-version: '21'`, `CLAUDE.md`, `docs/SPRINT-0.md`). Aucune API ni syntaxe postérieure à 21 trouvée dans `backend/src/main/java` (recherche ciblée : `StructuredTaskScope`, `ScopedValue`, `SequencedCollection`, `module-info`, aucun `--enable-preview` dans les `pom.xml`) — le retour ne nécessite donc aucune réécriture de code, uniquement les déclarations de version. `mvn -B clean verify`, `docker build` et `docker run --rm <image> java -version` vérifiés après correction.
- Décisions : le passage à 25 (session du 2026-08-24) ne reposait sur aucune décision tracée — ADR ou entrée de backlog — et Spring Boot 3.5 cible le terrain supporté Java 17-21. Il a en outre causé un incident réel : ArchUnit 1.3.0 ne sait pas lire le bytecode Java 25 (`Unsupported class file major version 69`) et échouait *silencieusement* à importer les classes analysées, laissant un test d'architecture vert sans avoir rien vérifié (voir la revue T-05 du même jour, `AucunRepositoryNeContourneBaseRepositoryTest`). Retour à 21 pour rester sur le terrain que l'écosystème (Spring Boot, ArchUnit, Hibernate) supporte réellement, plutôt que de courir après chaque outil qui n'a pas encore rattrapé la dernière version du JDK.
- Piège rencontré : les entrées de JOURNAL antérieures (2026-08-24) mentionnant le passage à Java 25 ne sont **pas réécrites** — le JOURNAL est un historique, pas un état courant ; cette entrée documente le retour en arrière sans effacer la trace de la décision précédente. Découverte en cours de vérification, **sans lien avec Java** : `docker run --rm <image> java -version` échouait avec « no main manifest attribute » — `backend/pom.xml` déclarait `spring-boot-maven-plugin` sans `<executions>` liant `repackage` à la phase `package` (câblage normalement hérité de `spring-boot-starter-parent`, absent ici car le parent est maison). Le jar produit n'a **jamais** été un fat jar exécutable depuis T-01 (109 Ko, aucun `Main-Class`) — vérifié identique en rejouant le build avec l'ancienne config Java 25, donc pas une régression du retour à 21. La CI ne fait que `docker build`, jamais `docker run` : le défaut n'aurait été détecté qu'au premier déploiement réel sur Render. Corrigé par l'ajout de l'`<execution><goal>repackage</goal></execution>` explicite ; jar repackagé vérifié à 85 Mo avec `Main-Class: org.springframework.boot.loader.launch.JarLauncher`, conteneur démarré avec succès jusqu'à l'échec attendu de connexion Postgres (aucune base disponible dans ce test manuel).

## 2026-08-25 (suite) — T-05 sous-tâches 12-13, PR #62 ouverte

- Fait : **sous-tâches 12-13 livrées** (`3aae349`, closes #61) — `AucuneDesactivationDuFiltreTest` (`enableFilter` uniquement dans `ArmeurFiltreEtablissement` et `VerificateurFiltreEtablissement`, `disableFilter` nulle part), `AucuneRequeteNativeSurEntiteMetierTest` (aucune requête native dans le code applicatif, sans chercher à deviner la table visée), `IsolationUtilisateursTest`. **`mvn -B -f backend/pom.xml clean verify` = 134 tests, vert.** Branche `feat/t-05-multi-etablissement` poussée, **PR #62 ouverte vers `main`** (6 commits) — CI Linux en cours au moment d'écrire, résultat non connu.
- Reste : **verdict de la CI de la PR #62** — premier passage sous Linux, c'est là que se jouent le garde-fou de résolution des sources et l'ordre d'exécution des tests. Ensuite fusion, puis **T-06 (audit et historisation)**. Non fait volontairement : `obtenirDansEtablissementCourant` et `listerParEtablissementCourant` **ne sont câblées à aucun endpoint** — prouvées au niveau service, pas par un test MockMvc avec `@PreAuthorize` ; la sécurité réelle se jouera au câblage. **Pour T-06 : les tables `_aud` d'Envers ne sont PAS soumises au `@Filter`** — l'historique d'une entité filtrée sera lisible sans filtrage tant que rien n'est prévu. Toujours ouvert : issue #58 (rate limiting, porte `securite:bloque-deploiement`, `deploy-production.yml` refuse effectivement de déployer tant qu'elle est ouverte), ADR-0008 stockage jetons (bloque T-11), hypothèse « Render écrase `X-Forwarded-For` ».
- Décisions : les deux tests statiques **comptent les fichiers analysés et échouent sous un seuil de 30** (70 aujourd'hui), avec un message annonçant une résolution de sources ratée et non une violation — sans ce garde-fou, un chemin non résolu sous Linux les rendait verts sans rien analyser, soit un test inerte protégeant une règle non négociable. `AucuneRequeteNativeSurEntiteMetierTest` interdit **toute** requête native plutôt que d'essayer de déterminer la table ciblée : analyser statiquement une chaîne SQL arbitraire n'est pas fiable, et c'est précisément le faux négatif à éviter.
- Piège rencontré : **une fuite réelle dans du code livré en T-04.** `UtilisateurService.obtenir(UUID)` n'était cadré par rien — tout appelant muni d'un identifiant obtenait n'importe quel compte, tous établissements confondus. Non exploitable aujourd'hui (seul appelant : `/moi`, toujours sur soi-même), mais le premier endpoint passant un identifiant d'URL aurait exposé tous les comptes de la plateforme. Corrigée par `obtenirDansEtablissementCourant` (vérifie l'affectation, répond « compte inexistant » sans révéler qu'un compte existe ailleurs) et `listerParEtablissementCourant`, qui manquait pour l'ADMIN. **Leçon : `Utilisateur` n'est pas filtrée — elle est lue pendant le login, avant qu'un contexte existe — donc son cloisonnement ne repose sur aucun filet automatique, uniquement sur `AffectationEtablissement`. C'est le point le plus exposé du modèle, et le seul où l'oubli ne se voit pas.**

## 2026-08-25 — T-05 — sous-tâche 11 livrée (critère de fin du Sprint 0)

- Fait : **sous-tâche 11 terminée et vérifiée** — `mvn -B -f backend/pom.xml clean verify` = **122 tests**, vert. `IsolationEtablissementTest` couvre D1 (parcours du metamodel : toute entité est `EntiteEtablissement` ou figure dans `HORS_PERIMETRE` = `Etablissement`, `Utilisateur`, `AffectationEtablissement`, `JetonRafraichissement`), D2 (toute entité filtrée a une fabrique dans `FabriquesEntitesTest` — aujourd'hui `DemoEntite` seule), C1-C8 pour chaque entité filtrée, plus (a) la garde AOP prouvée bout-en-bout en contexte Spring réel et (b) l'inatteignabilité des méthodes de suppression depuis les interfaces de repository. **R4.4 implémentée** : `RemplisseurEtablissement` gagne un hook `@PreUpdate` qui refuse et journalise toute modification dont l'`etablissementId` diffère du contexte courant — seul `@PrePersist` existait, alors que la sous-tâche 7 annonçait R4.1 à R4.5. Quatre cas unitaires ajoutés à la granularité de R4.3 (contexte identique, différent, absent, SUPER_ADMIN). Branche `feat/t-05-multi-etablissement` créée, deux commits : `c5f7ce7` (sous-tâche 11 + R4.4), `1b277e9` (passage à Java 25). Rien n'est poussé.
- Reste : **sous-tâches 12-13** (`AucuneDesactivationDuFiltreTest`, `AucuneRequeteNativeSurEntiteMetierTest`, `IsolationUtilisateursTest`, `UtilisateurService` cadré par affectation), puis PR vers `main`. **Pour T-06 : les tables `_aud` d'Envers ne sont PAS soumises au `@Filter`** — l'historique d'une entité filtrée est donc lisible sans filtrage tant que rien n'est prévu ; à traiter explicitement au moment d'activer Envers, pas à découvrir après. Toujours ouvert : les deux issues GitHub (rate limiting bloque le déploiement, ADR-0008 stockage jetons bloque T-11) et l'hypothèse « Render écrase `X-Forwarded-For` ».
- Décisions : C8 **exige** désormais le refus au lieu de constater la lacune — un test qui verrouille un défaut comme correct en empêche la correction future ; c'est ce constat qui a déclenché l'implémentation de R4.4 plutôt que son report. Le test SUPER_ADMIN de R4.5 verrouille une **absence** : `RemplisseurEtablissement` n'a jamais lu `PerimetreEtablissement.origine()`, et le test empêche qu'une dérogation en écriture soit introduite plus tard par confusion avec la désactivation ciblée du filtre en lecture. Passage à **Java 25** assumé partout (pom, images Docker, CI, docs) : le JOURNAL de T-01 l'annonçait déjà, le `pom.xml` était resté en 21 — la baseline pré-upgrade n'a pas pu être exécutée faute de JDK 21 local, la validation repose donc sur les 122 tests en Java 25. `docs/SPRINT-0.md` corrigé de Spring Boot 4.0.x vers 3.5.16, la version réellement utilisée.
- Piège rencontré : **le test existant n'était pas creux — c'est la campagne qui était polluée.** `IsolationEtablissementTest` était vert en isolation et rouge en campagne complète (4 échecs). Cause : `FuiteContexteEtablissementTest`, test unitaire pur, substituait `ContexteEtablissement.entityManagerFactory` — champ **statique, partagé par toute la JVM** — puis le remettait inconditionnellement à `null` en `@AfterEach`. Le contexte Spring étant mis en cache entre classes de test, les classes suivantes héritaient d'un champ nul ; `ouvrir()`/`restaurer()` ne pouvaient plus ré-armer le filtre Hibernate (`reArmerFiltreSurSessionLiee()` est un no-op silencieux si la fabrique est nulle) et les lectures inter-établissement passaient. Corrigé par sauvegarde/restauration de la valeur d'origine. **Leçon : un état statique substitué par un test unitaire contamine toute la campagne, et l'ordre d'exécution décide de qui en meurt** — un test vert en isolation ne prouve rien. À surveiller à la première exécution CI sous Linux, le cache du contexte Spring pouvant s'y ordonner autrement.

## 2026-08-24 — T-05 (contexte multi-établissement) — sous-tâches 1 à 10 livrées, 11 en cours

- Fait : socle multi-établissement complet et vert (`mvn -B -f backend/pom.xml clean verify` = **106 tests**, dernier état vérifié par moi-même). Sous-tâches 1-6 (contexte ThreadLocal, filtre Hibernate armé par session, `ContexteEtablissementFilter`, `Etablissement` minimale, V4 + conversion `db/testdata` en `R__`, ADR-0002 mis à jour) ; 7-10 (`RemplisseurEtablissement` R4.1-R4.5, `BaseRepositoryImpl.findById` en Criteria + `JpaRepositoriesConfig`, `GardeContexteEtablissement` AOP, `ContexteEtablissementTaskDecorator` + `AsyncConfig`, `DemoEntite` passée sous `EntiteEtablissement`, registre `FabriquesEntitesTest`).
- Reste : **sous-tâche 11 `IsolationEtablissementTest` — critère de fin du Sprint 0, NON TERMINÉE.** Le fichier existe (596 lignes) avec `ControleurSecuriseDeTestIsolation` (41 lignes), écrits par l'agent `testeur` qui s'est **arrêté sans compte rendu** : **rien n'est vérifié — ni que le test compile, ni qu'il passe, ni qu'il teste quoi que ce soit.** Premier geste de la prochaine session : `mvn -B -f backend/pom.xml clean verify`. Le brief exigeait D1 (parcours du metamodel : toute entité est `EntiteEtablissement` ou dans la liste courte hors périmètre), D2 (toute entité filtrée a une fabrique), C1-C8, plus deux preuves manquantes signalées en sous-tâche 10 : (a) garde AOP en contexte Spring réel, (b) aucune méthode de suppression atteignable depuis les interfaces de repository. **Exigence non négociable : pour chaque cas, casser le mécanisme, vérifier que le test vire au rouge, rétablir.** Ensuite sous-tâches 12-13 (`AucuneDesactivationDuFiltreTest`, `AucuneRequeteNativeSurEntiteMetierTest`, `IsolationUtilisateursTest`, `UtilisateurService` cadré par affectation) puis 14 (JOURNAL + noter pour T-06 que les tables `_aud` d'Envers ne sont PAS soumises au `@Filter`). **Rien n'est commité** : tout T-05 est en working tree sur `main`, et la convention interdit de commiter sur `main` — créer `feat/t-05-multi-etablissement`. Toujours ouvert : les deux issues GitHub (rate limiting bloque le déploiement, ADR-0008 stockage jetons bloque T-11) et l'hypothèse « Render écrase `X-Forwarded-For` ».
- Décisions : `EntiteEtablissement` intercalée sous `BaseEntity` plutôt qu'un champ nullable sur `BaseEntity` — 4 entités ne peuvent pas être filtrées (`Etablissement`, `Utilisateur`, `AffectationEtablissement`, `JetonRafraichissement`, ces deux dernières lues *pendant* le login) ; un champ nullable s'oublie, deux superclasses rendent l'oubli énumérable par le metamodel. `findById` réécrit en Criteria dans `BaseRepositoryImpl` — `EntityManager.find()` ignore les `@Filter` (angle mort A1 de l'ADR-0002). `DetecteurFuiteContexteFilter` actif dans **tous** les profils : purge + ERROR partout, lève en profil `test` uniquement — la fuite ThreadLocal ne survient que sous charge concurrente, qu'aucun test ne reproduit. `RemplisseurEtablissement` déplacé dans `common.domain` pour rendre `setEtablissementId` package-private : la règle « personne n'écrit `etablissement_id` à la main » est désormais tenue par le compilateur, comme `BaseRepository` sans `delete` (vérifié par mutation : un appel depuis `DemoEntite` ne compile pas). `db/testdata` en migrations `R__` avec `ON CONFLICT DO UPDATE` — le fichier reste la source de vérité ; `date_creation` jamais dans le SET.
- Piège rencontré : **deux faux positifs, tous deux sur le mécanisme central.** (1) Un `V2__common_demo_entites.sql` supprimé subsistait dans `target/classes` : le build était vert sur un état qui n'existait nulle part. D'où `mvn -B clean verify` désormais dans `ci.yml` — le `clean` n'est pas négociable. (2) `VerificateurFiltreEtablissement` appelait `getEntityDescriptor(String)` et levait `UnknownEntityTypeException` : jamais exercé tant qu'aucune entité n'étendait `EntiteEtablissement`, donc 96 tests verts sur un vérificateur cassé. Corrigé en sous-tâche 10 via l'overload `Class<?>`. **Leçon : un socle qu'aucune entité réelle n'utilise ne prouve rien, quel que soit le nombre de tests verts** — c'est pourquoi la sous-tâche 11 exige la vérification par mutation.

## 2026-08-22 — Reprise (aucun code)

- Fait : rien d'implémenté. Session de reprise de contexte uniquement (relecture JOURNAL + SPRINT-0). État inchangé depuis `901fed4`, working tree propre sur `main`.
- Reste : **T-05 — Contexte multi-établissement**, non entamé (`etablissement_id` sur `BaseEntity` + remplissage auto à la persistance, résolution de l'établissement courant depuis le JWT vers le contexte de sécurité, filtre Hibernate activé sur chaque session, ouverture explicite de contexte pour les traitements async/planifiés, désactivation ciblée pour `SUPER_ADMIN` limitée aux entités établissement et utilisateur ; critère de fin = test d'isolation générique parcourant toutes les entités métier, y compris accès direct par identifiant et cas du SUPER_ADMIN basculé). Toujours ouvert par ailleurs : les deux issues GitHub à créer depuis `issue-rate-limiting.md` et `issue-stockage-jetons.md` (rate limiting bloque le déploiement, ADR-0008 stockage des jetons bloque T-11), et l'hypothèse « Render écrase `X-Forwarded-For` » non vérifiée en recette.
- Décisions : aucune nouvelle.
- Piège rencontré : sans objet.

## 2026-08-21 — T-04

- Fait : module `identite` complet (entités `Utilisateur`/`AffectationEtablissement`/`JetonRafraichissement`, RBAC en mémoire `RoleCode`/`Permission`, JWT access 15 min + refresh 7 jours avec rotation et révocation de famille, `SecurityConfig` deny-by-default), migration `V3__identite_rbac.sql` + données de démo ; `GET /api/v1/utilisateurs` paginé (enveloppe T-03) avec liste blanche de champs triables ; `JournalSecurite` (`common/securite/`) journalise les échecs d'auth/rejets de jeton sans jamais email, mot de passe ni jeton en clair (UUID ou empreinte SHA-256 tronquée uniquement) ; 84 tests (unitaires + intégration MockMvc/PostgreSQL), tous verts.
- Reste : T-05 à T-11. Rate limiting sur `/auth/login`/`/refresh` (issue-rate-limiting.md, bloque le déploiement) et décision stockage des jetons côté frontend (issue-stockage-jetons.md, ADR-0008 à écrire, bloque T-11) — deux issues GitHub à créer à partir des fichiers `.md` à la racine, pas encore faites. Hypothèse Render écrase `X-Forwarded-For` non vérifiée en recette (voir commentaire `application.yml`) — à confirmer avant de s'appuyer dessus pour la limitation par IP. Critères US-04 "création de compte"/"attribution de rôle" repoussés au Sprint 1 (décision explicite, pas un oubli).
- Décisions : deux permissions distinctes `UTILISATEUR_GERER` (ADMIN, borné à son établissement) et `UTILISATEUR_GERER_PLATEFORME` (SUPER_ADMIN seul, comptes de tous les établissements) — une permission unique partagée avait fuité l'accès multi-établissement à ADMIN (trouvé en revue). `Utilisateur` n'a pas d'`etablissement_id` : légitime, pas une exception à la règle CLAUDE.md 2 — c'est `AffectationEtablissement` qui porte la relation n-n avec l'établissement (ADR-0002). Logs de rejet de jeton en DEBUG uniquement pour `jeton_expire` (fréquent, attendu), WARN pour tout le reste (signature forgée = incident). `server.forward-headers-strategy: framework` activé pour que `getRemoteAddr()` reflète le client réel derrière le proxy Render.
- Piège rencontré : la revue a nécessité 3 repasses successives — chaque correction de sécurité a introduit ou révélé un nouveau défaut avant d'être clean (fuite d'email via `MethodArgumentNotValidException` non gérée → assertion de test trop faible sur `getFormattedMessage()` seul, sans la stack trace → fuite ADMIN/SUPER_ADMIN via permission partagée → log DEBUG masquant les rejets de jeton suspects → tests de niveau de log et de liste blanche de tri manquants). Prévoir systématiquement une repasse de revue après toute correction touchant permissions ou journalisation, pas seulement après l'implémentation initiale.

## 2026-08-17 — T-03

- Fait : enveloppe paginée maison `PageReponse` + `PaginationUtils` (bornes, tri via `Pageable`/`Sort` natif Spring Data) ; `SpecificationsBase` (filtres dynamiques réutilisables) ; `ExcelExporter` générique (Apache POI) ; contrôleur/entité de démo (`/internal/demo/entites`, profils `local`/`test`) avec 25 tests (unitaires + intégration MockMvc contre PostgreSQL réel).
- Reste : T-04 à T-11, rien d'autre entamé volontairement. Points "important"/"mineur" de la revue non traités (service manquant dans le contrôleur de démo, `Specification.where` déprécié, `ExcelExporter` non streamé pour gros volumes, absence de liste blanche des champs triables/filtrables, dépendance POI en dur dans `backend/pom.xml` au lieu du parent).
- Décisions : `BaseRepository<T> extends Repository<T, UUID>` créé dans `common/repository/` — n'hérite ni de `JpaRepository`, ni `CrudRepository`, ni `JpaSpecificationExecutor` (ce dernier expose `delete(Specification)`, une suppression en masse qui contourne Envers et le futur filtre multi-établissement) ; méthodes de lecture par spécification redéclarées à la main (`findOne`, `findAll` ×3, `count`, `exists`) + `existsById` ; second paramètre générique `ID` supprimé au profit d'`UUID` fixe, conforme à la règle UUID v7 systématique — désormais documenté dans `CLAUDE.md` (règle 4) comme modèle obligatoire pour tous les futurs repositories métier. Données de démonstration dans `db/testdata/` (nouvelle convention, distincte de `db/migration/`), chargées uniquement via `spring.flyway.locations` en profils `local`/`test` — jamais en production.
- Piège rencontré : `?sort=libelle,desc` scindé par Spring MVC (convertisseur String→Collection sur la virgule) avant validation par `PaginationUtils` avait produit une 500 lors des premiers tests d'intégration — corrigé en repassant par le support natif `Pageable`/`@PageableDefault` de Spring Data Web plutôt que par lecture manuelle de `HttpServletRequest` (contournement fragile écarté en revue). Le test d'intégration dépend d'un PostgreSQL local (docker-compose, base `edukeys_test` à créer manuellement) tant que Testcontainers (T-08) n'est pas en place — sera rouge en CI jusque-là.

## 2026-08-16 — T-02
- Fait : package `common/` complet — `BaseEntity` (id UUID v7 via générateur personnalisé, `actif`, audit JPA), `EntiteDesactivable` ; hiérarchie d'exceptions (`RessourceIntrouvableException` 404, `RegleMetierViolee` 422, `ConflitException` 409, `AccesInterditException` 403) + `@RestControllerAdvice` en `ProblemDetail` RFC 7807 avec `correlationId` ; `CorrelationIdFilter` (MDC + en-tête `X-Correlation-Id`) ; interface `Notificateur` + enum `TypeNotification` (drapeau `eligibleSms`, sans implémentation de canal).
- Reste : T-03 à T-11, rien d'autre entamé volontairement.
- Décisions : UUID v7 via `@UuidGenerator(algorithm = UuidV7Generator.class)` — Hibernate 6.6.53.Final (embarqué par Spring Boot 3.5.16) n'expose que `AUTO/RANDOM/TIME`, pas encore `Style.VERSION_7`, donc générateur personnalisé (`common/domain/UuidV7Generator`) plutôt qu'attendre une montée de version ; `AuditorAware` renvoie `"system"` en dur jusqu'à T-04 (pas de contexte de sécurité avant).
- Piège rencontré : `@PathVariable` sans nom explicite levait `IllegalArgumentException` (masqué en 500 par le handler générique) faute du flag compilateur `-parameters` — corrigé par `maven.compiler.parameters=true` dans le pom parent. Vérifié en conditions réelles : `docker compose up -d` + `mvn spring-boot:run` → Flyway valide contre Postgres 18.6, `/actuator/health` répond `UP`.

## 2026-08-15 — T-01
- Fait : squelette Maven (pom parent + module `backend`, Spring Boot 3.5.16 / Java 25) ; `docker-compose.yml` avec PostgreSQL 18 + volume persistant ; Flyway `V1__init.sql` (`schema_test`) ; profils `local`/`test` ; `/actuator/health` vérifié `UP` contre le Postgres de docker-compose.
- Reste : tout T-02 à T-11, rien d'autre entamé volontairement.
- Décisions : Postgres 18 (pas 16 comme suggéré dans SPRINT-0.md) pour coller à CLAUDE.md ; volume monté sur `/var/lib/postgresql` (pas `/var/lib/postgresql/data`) — l'image Postgres 18+ a changé sa convention de stockage et refuse de démarrer sur l'ancien point de montage.
- Piège rencontré : `taskkill /F /IM java.exe` en fin de session a tué tous les process Java de la machine (5), pas seulement l'appli Spring Boot lancée pour le test — à éviter, cibler le PID exact la prochaine fois.
